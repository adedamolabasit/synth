# Synth Frontend

This is the frontend for the Synth project.  
It uses **Vite + React** and connects to the backend API.

---

## 📦 Installation

Clone the repository:

npm install

## Create a .env file

```bash
cp .env.example .env
```
## Start the app

npm run dev
